# 飞书应用配置
APP_ID=cli_xxxxx
APP_SECRET=xxxxx

# Claude Agent HTTP 后端配置
# Docker 网络中使用服务名，本地开发使用 localhost
CLAUDE_AGENT_URL=http://claude-agent-http:8000
# 超时时间（秒）：建议 300（5分钟）或 600（10分钟）
# 对于复杂任务（长文本、工具调用等），可能需要更长时间
CLAUDE_AGENT_TIMEOUT=300

# 会话映射存储目录
# LOCAL_SESSION_DIR: 宿主机路径（可自定义，如 /data/lark 或 ~/.claude-lark）
LOCAL_SESSION_DIR=~/.claude-lark
# 注意：容器内路径固定为 /data/claude-lark，无需配置

# 可选配置
TZ=Asia/Shanghai
