# 飞书应用配置
APP_ID=cli_xxxxx
APP_SECRET=xxxxx

# Claude Agent HTTP 后端配置
# 注意：使用 Docker Compose 时，使用服务名 http://claude-agent-http:8000
#       使用 run.sh 时，使用宿主机地址 http://localhost:8000
CLAUDE_AGENT_URL=http://localhost:8000
CLAUDE_AGENT_TIMEOUT=120

# 会话映射存储目录
# LOCAL_SESSION_DIR: 宿主机路径（可自定义，如 /data/lark 或 ~/.claude-lark）
LOCAL_SESSION_DIR=~/.claude-lark
# 注意：容器内路径固定为 /data/claude-lark，无需配置

# 可选配置
TZ=Asia/Shanghai
